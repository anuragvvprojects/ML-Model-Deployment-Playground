apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "minimal-deploy.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "minimal-deploy.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "minimal-deploy.name" . }}
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
