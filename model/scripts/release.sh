#!/usr/bin/env bash
set -euo pipefail
echo "Tagging and pushing image (example)"
# git tag v$(date +%Y.%m.%d.%H%M); git push --tags
# docker build -t your-repo/minimal-deploy:latest -f infra/Dockerfile .
# docker push your-repo/minimal-deploy:latest


